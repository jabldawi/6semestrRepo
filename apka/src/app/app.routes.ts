import { Routes } from '@angular/router';
import { LogowanieComponent } from './logowanie/logowanie.component';

export const routes: Routes = [
    { path: '', component: LogowanieComponent },
    // Dodaj inne ścieżki, jeśli są potrzebne
  ];
